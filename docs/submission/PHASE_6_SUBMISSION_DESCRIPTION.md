# SecureOps Guardian submission description

SecureOps Guardian turns an ordinary-language request about an exact GitHub change into a bounded security investigation and, only for one proven Kubernetes NetworkPolicy pack, a sandbox-verified least-privilege remediation under human-controlled write boundaries.

The primary demo starts with the public checkout regression commit URL. Two bounded TrueForge child threads join real GitHub commit/file evidence to explicitly synthetic deployment, alert, reachability, and dependency observations. Guardian reports a cited `High` `SEC-NET-001` finding while keeping actual data access and exfiltration `Unknown`. A pinned verifier pack runs in one Daytona sandbox across last-known-good, suspect, deny-all, and Guardian candidate states. It reproduces unrestricted egress, rejects deny-all because it breaks the required PostgreSQL path, and accepts the exact least-privilege policy.

The stock TrueForge OpenUI Incident Brief presents the finding, causal chain, evidence IDs, four-state proof, candidate/proposal/pack bindings, limitations, receipt, and real PR link. The completed release rehearsal performs fresh remote reconciliation and returns the already-open [checkout PR #1](https://github.com/jayesh9747/guardian-demo-checkout/pull/1) as `PR_REUSED`, with zero write calls and zero approval events. The preserved historical creation trace shows that `create_branch`, `create_or_update_file`, and `create_pull_request` each had a separate TrueForge approval boundary; the current safe fixture is not destructively reset to manufacture new writes.

Breadth is proven without overstating remediation. The public `guardian-demo-privileged-api` commit produces five exact high-severity Pod Security Standards findings through `k8s-workload-security-v1`, then stops at GitHub-only `ANALYSIS_ONLY`. The separate benign `guardian-demo-orders-egress` commit returns the exact terminal status `NO_DETERMINISTIC_FINDING`. Both keep runtime claims `Unknown`; neither can enter Fixture evidence, Daytona, proposal, approval, or write routes. Three consecutive fresh-session checkout rehearsals returned the same exact PR/proposal/pack result with no mutation, and a separate confirmation-cancellation trace produced zero agents, MCP calls, sandboxes, approvals, or writes.

TrueForge is essential and visible: model routing, natural-language interpretation, dynamic subagents, official and custom MCP connectors, Daytona execution, persistent sessions, human tool approvals, stock Generative UI, and the Investigation rail all carry part of the auditable journey. Guardian never merges, deploys, accesses Kubernetes, resets a fixture, or claims that static verification proves admission, CNI behavior, live reachability, data access, or exfiltration.

AI assistants supported planning, implementation, regression tests, evidence capture, documentation, and the independent standards/spec review. The operator retained responsibility for repository credentials, TrueForge/provider configuration, confirmation and approval decisions, evidence acceptance, public visibility, final recording, merge, and submission. Qodo status is reported truthfully in the [release evidence](../evidence/EXPANSION_PHASE_5_EVALUATION_DEMO_AND_RELEASE.md); a paused response is never described as approval.

Known limits: remediation is restricted to the pinned `k8s-network-egress-v1@1.0.4` static subset and the owned checkout scenario. Workload analysis covers only the documented Pod/Deployment security-context subset and has no remediation path. Operational evidence is synthetic. The current public state proves exact PR reuse, not a new live creation or first-write denial. Cold model rehearsals took approximately six to seven minutes; the accepted interactive walkthrough of the completed canonical session took `168.3` seconds, while an initial `184.2`-second attempt was rejected. The submission is not a claim of three-minute cold execution. Final recording upload, public permission selection, signed-out playback validation, merge, and submission remain operator actions.
