# Phase 7 — unified SecureOps Guardian

## Goal

Replace the phase-wise user experience with one saved TrueForge agent named `secureops-guardian` while composing, not weakening, the Phase 2-6 typed contracts.

## Entry gate

- Phase 6 PR #7 is merged after operator acceptance.
- Product work starts from updated `main` on `phase-7/unified-guardian`.
- Fixture PR `jayesh9747/guardian-demo-checkout#1` remains open and unmerged at `44fb8c7f5e99f835c6779f5e7b777c1b016af5b3`.
- The operator-owned TrueForge `docker-compose.yml` change remains untouched.

## Required product contract

1. One schema-version-1 repository scope and three hard-ceiling modes.
2. Parameterized official GitHub MCP read-only preflight for any authorized repository.
3. Exact supported-fixture routing through the retained Phase 2-6 modules.
4. `INCONCLUSIVE` before Daytona/proposal/approval/write for missing, conflicting, or unsupported requirements.
5. Explicit remediation allowlist and documented NetworkPolicy static subset.
6. Exact proposal, unchanged-base, candidate, remote-content, and separate-approval gates for `OPEN_PR`.
7. Stock OpenUI or complete Markdown plus one machine-readable run receipt.
8. No merge, deployment, cluster access, overwrite, destructive reset, or atomic-write claim.

## Exit gate

- One exported and saved agent named `secureops-guardian`, with ID recorded.
- Current fixture, arbitrary analysis, and safe `OPEN_PR` prompt templates.
- Required Phase 7 adversarial/mode tests plus all Phase 2-6 suites and matrices pass.
- README, architecture, status, migration, and Phase 7 evidence are current.
- Qodo review is requested; if paused, an alternate review is completed and remediated.
- A non-draft Phase 7 PR is open.

The retained-feature roadmap remains in [`PHASE_7_RETAINED_FEATURES.md`](./PHASE_7_RETAINED_FEATURES.md) as historical post-core backlog. It no longer defines the Phase 7 user-facing implementation.
