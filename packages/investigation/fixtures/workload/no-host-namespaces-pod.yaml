apiVersion: v1
kind: Pod
metadata:
  name: no-host-namespaces
  namespace: tests
spec:
  hostNetwork: false
  hostPID: false
  containers:
    - name: app
      image: example.test/app:1
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: [ALL]
