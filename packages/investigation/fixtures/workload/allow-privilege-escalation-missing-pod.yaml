apiVersion: v1
kind: Pod
metadata:
  name: missing-ape
  namespace: tests
spec:
  containers:
    - name: app
      image: example.test/app:1
      securityContext:
        privileged: false
        capabilities:
          drop: [ALL]
