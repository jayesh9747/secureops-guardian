apiVersion: v1
kind: Pod
metadata:
  name: false-ape
  namespace: tests
spec:
  containers:
    - name: app
      image: example.test/app:1
      securityContext:
        allowPrivilegeEscalation: false
        privileged: false
        capabilities:
          drop: [ALL]
