apiVersion: v1
kind: Pod
metadata:
  name: safe-volume
  namespace: tests
spec:
  containers:
    - name: app
      image: example.test/app:1
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: [ALL]
  volumes:
    - name: scratch
      emptyDir: {}
