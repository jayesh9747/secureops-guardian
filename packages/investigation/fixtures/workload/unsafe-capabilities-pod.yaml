apiVersion: v1
kind: Pod
metadata:
  name: unsafe-capabilities
  namespace: tests
spec:
  containers:
    - name: app
      image: example.test/app:1
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          add: [SYS_ADMIN]
